# 📊 iM Digital Banker - 모델 요약

> 최종 업데이트: 2025-01-19

---

## 🏆 주요 성과 요약

| KPI | LightGBM R² | SMAPE | 등급 |
|-----|-------------|-------|------|
| **카드총사용** | **86.3%** | 1.17% | A |
| **예금총잔액** | 83.1% | 1.04% | A |
| **FX총액** | 82.9% | 1.88% | A |
| **대출총잔액** | 81.3% | 0.87% | A |
| **디지털거래금액** | 81.3% | 1.13% | A |
| **순유입** | 13.4% | 1.75% | C |

> **참고**: 순유입은 양/음 값이 섞인 변동성 높은 지표로 예측이 어려움

### 추천 시스템 성과

| 항목 | 수치 | 비고 |
|------|------|------|
| 총 액션 수 | **2,582개** | 세그먼트별 맞춤 추천 |
| 액션 유형 | **6개** | 다양한 비즈니스 상황 커버 |
| 세그먼트 커버리지 | **100%** | 2,038개 전체 커버 |
| Watchlist 알림 | **44개** | 21개 업종 분포 |
| severity 분포 | 4개 레벨 | CRITICAL 17, HIGH 13, MEDIUM 10, LOW 4 |

---

## 1. 모델 아키텍처 개요

### 1.1 LightGBM 예측 모델 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                    LightGBM 예측 시스템                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────────────────────────────────────┐                 │
│   │         LightGBM (6개 KPI)               │                 │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │                 │
│   │  │ 예금총잔액 │ │ 대출총잔액 │ │ 카드총사용 │ │                 │
│   │  │ R²=83.1% │ │ R²=81.3% │ │ R²=86.3% │ │                 │
│   │  └──────────┘ └──────────┘ └──────────┘ │                 │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │                 │
│   │  │디지털거래 │ │   순유입  │ │  FX총액  │ │                 │
│   │  │ R²=81.3% │ │ R²=13.4% │ │ R²=82.9% │ │                 │
│   │  └──────────┘ └──────────┘ └──────────┘ │                 │
│   └──────────────────────────────────────────┘                 │
│                                                                 │
│   ┌──────────────────────────────────────────┐                 │
│   │      딥러닝 모델 (추가 구현)              │                 │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │                 │
│   │  │   LSTM   │ │Transformer│ │   TFT    │ │                 │
│   │  │  구현완료 │ │  구현완료  │ │ 구현완료  │ │                 │
│   │  └──────────┘ └──────────┘ └──────────┘ │                 │
│   └──────────────────────────────────────────┘                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. KPI별 모델 성능

### 2.1 LightGBM 성능 (Horizon 1개월)

| KPI | R² | SMAPE | RMSE | 등급 |
|-----|-----|-------|------|------|
| **카드총사용** | 0.863 | 1.17% | 0.51 | 🟢 우수 |
| **예금총잔액** | 0.831 | 1.04% | 1.04 | 🟢 우수 |
| **FX총액** | 0.829 | 1.88% | 0.07 | 🟢 우수 |
| **대출총잔액** | 0.813 | 0.87% | 1.50 | 🟢 우수 |
| **디지털거래금액** | 0.813 | 1.13% | 1.26 | 🟢 우수 |
| **순유입** | 0.134 | 1.75% | 2.57 | 🟡 개선 필요 |

### 2.2 Horizon별 성능 변화

| KPI | H1 R² | H2 R² | H3 R² | 평균 |
|-----|-------|-------|-------|------|
| 카드총사용 | 0.863 | 0.851 | 0.842 | 0.852 |
| 예금총잔액 | 0.831 | 0.815 | 0.798 | 0.815 |
| FX총액 | 0.829 | 0.820 | 0.811 | 0.820 |
| 대출총잔액 | 0.813 | 0.795 | 0.780 | 0.796 |
| 디지털거래금액 | 0.813 | 0.802 | 0.790 | 0.802 |
| 순유입 | 0.134 | 0.118 | 0.110 | 0.121 |

---

## 3. 피처 엔지니어링

### 3.1 피처 카테고리

| 카테고리 | 피처 수 | 예시 |
|----------|---------|------|
| **기본 KPI** | 6 | 예금총잔액, 대출총잔액, 순유입 등 |
| **Lag 피처** | ~48 | *_lag1, *_lag2, *_lag3 |
| **Rolling 피처** | ~36 | *_roll3_mean, *_roll6_mean |
| **변화율 피처** | ~12 | *_pct_chg, *_mom |
| **거시경제 변수** | 84 | ESI, Fed금리, 환율, 유가, 부동산 등 |
| **거시경제 Lag** | 96 | 32개 변수 x 3개 Lag |
| **세그먼트 피처** | 4 | 연령대, 직업군, 거래활성도, 자산규모 |

### 3.2 총 피처 수

- **학습 피처**: ~245개
- **패널 컬럼**: ~332개 (메타 컬럼 포함)

---

## 4. 하이퍼파라미터

### 4.1 LightGBM

```python
lgbm_params = {
    'n_estimators': 500,
    'learning_rate': 0.05,
    'num_leaves': 31,
    'max_depth': 8,
    'min_child_samples': 20,
    'subsample': 0.8,
    'colsample_bytree': 0.8,
    'reg_alpha': 0.1,
    'reg_lambda': 0.1,
    'random_state': 42,
}
```

### 4.2 XGBoost

```python
xgb_params = {
    'n_estimators': 500,
    'learning_rate': 0.05,
    'max_depth': 6,
    'min_child_weight': 5,
    'subsample': 0.8,
    'colsample_bytree': 0.8,
    'reg_alpha': 0.1,
    'reg_lambda': 0.1,
    'random_state': 42,
}
```

### 4.3 CatBoost

```python
catboost_params = {
    'iterations': 500,
    'learning_rate': 0.05,
    'depth': 6,
    'l2_leaf_reg': 3,
    'random_seed': 42,
    'verbose': False,
}
```

---

## 5. 데이터 변환

### 5.1 타겟 변환

| KPI | 변환 | 역변환 |
|-----|------|--------|
| 예금총잔액 | `log1p(x)` | `expm1(x)` |
| 대출총잔액 | `log1p(x)` | `expm1(x)` |
| 순유입 | `sign(x) * log1p(abs(x))` | `sign(x) * expm1(abs(x))` |
| 카드총사용 | `log1p(x)` | `expm1(x)` |
| 디지털거래금액 | `log1p(x)` | `expm1(x)` |
| FX총액 | `log1p(x)` | `expm1(x)` |

### 5.2 스케일 보정

- 예측 후 실제 스케일로 복원 시 보정 계수 적용
- 보정 범위: x1.27 ~ x1.76

---

## 6. 파일 구조

```
src/imradar/models/
├── forecast_lgbm.py          # LightGBM 단일 모델 (현재 기본)
├── backtest.py               # 백테스트 유틸
├── residual_alerts.py        # 잔차 기반 이상 탐지
├── risk_radar.py             # 리스크 레이더
├── hierarchical.py           # 계층적 조정 (MinT)
└── fx_regime.py              # FX 레짐 분류

scripts/
├── run_mvp.py                # 메인 파이프라인
├── deep_learning_models.py   # LSTM/TFT/Transformer 모델
├── advanced_features.py      # 고급 피처/XAI
└── transformer_tuning.py     # Transformer 하이퍼파라미터 튜닝

outputs/
├── forecast_metrics.csv      # 모델 성능 메트릭
├── actions_enhanced.csv      # 전체 추천 액션
├── watchlist_alerts.csv      # 리스크 알림 (44개, 4개 severity)
├── deep_learning/
│   ├── lstm_model.pt         # 학습된 LSTM
│   ├── tft_model.pt          # 학습된 TFT
│   └── transformer_model.pt  # 학습된 Transformer
└── dashboard/                # HTML 대시보드
```

---

## 7. 사용법

### 7.1 파이프라인 실행

```bash
cd code
source venv/bin/activate
python scripts/run_mvp.py \
    --raw_csv "../외부 데이터/iMbank_data.csv.csv" \
    --external_dir "../외부 데이터"
```

### 7.2 딥러닝 모델 실행 (선택)

```bash
python scripts/deep_learning_models.py \
    --raw_csv "../외부 데이터/iMbank_data.csv.csv" \
    --external_dir "../외부 데이터"
```

---

## 8. 향후 개선 방향

| 우선순위 | 개선 사항 | 예상 효과 |
|----------|----------|----------|
| 1 | 순유입 전용 피처 엔지니어링 | R² +10~20% |
| 2 | 딥러닝 모델 (LSTM/TFT) 통합 | 순유입 R² 개선 가능 |
| 3 | Temporal Fusion Transformer 튜닝 | 장기 예측 개선 |
| 4 | 실시간 모델 업데이트 파이프라인 | 운영 효율성 |

---

## 9. 결론

### 핵심 성과
- **6개 KPI** 동시 예측 가능
- **LightGBM** 기반 안정적인 성능 (R² 81~86%)
- **순유입 제외** 모든 KPI R² 80% 이상 달성
- **2,582개** 맞춤형 액션 추천

### 모델 등급 요약
- 🟢 **우수** (R² > 0.8): 예금, 대출, 카드, 디지털, FX
- 🟡 **개선 필요** (R² < 0.3): 순유입 (변동성 높은 지표 특성)

### 추천 시스템 성과
- **6개 액션 유형**: DEPOSIT_GROWTH, LIQUIDITY_STRESS, ENGAGEMENT_DROP, GROWTH_OPPORTUNITY, FX_EXPANSION, CREDIT_RISK
- **44개 Watchlist 알림**: 21개 업종 분포, 4개 severity 레벨

---

*문서 작성: iM-ONEderful Team*
